const fs = require('fs'); fetch('http://localhost:5000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({username: 'admin', password: 'adminpassword'}) }).then(r => r.json()).then(data => { const token = data.token; fetch('http://localhost:5000/api/it-assets/reports/assets.pdf', { headers: { Authorization: 'Bearer ' + token } }).then(r => { console.log(r.status); return r.arrayBuffer() }).then(b => fs.writeFileSync('assets_final.pdf', Buffer.from(b))); });
