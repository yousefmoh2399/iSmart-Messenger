# Desktop App

Flutter Windows app for workplace document access and management.

## Run

```bash
flutter pub get
flutter run -d windows --dart-define=API_BASE_URL=https://usta.qzz.io
```

## Features

- login with username/password
- list current user's files only
- search and sort
- open / download / rename / delete
- admin user can create more users from the dashboard
